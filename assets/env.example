# Save API keys locally. Never paste them into chat or commit the real .env.
VINTED_API_KEY=sk-replace-locally
# Optional additional workers. Numbered slots may skip values.
# VINTED_API_KEY_2=sk-replace-second-locally
# VINTED_API_KEY_3=sk-replace-third-locally
